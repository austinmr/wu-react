import API_KEY from '../__env'

export const getConditions = (city, state) => {
	// DEFINE A FUNCTION TO RETRIEVE CURRENT CONDITIONS 
	// ACCESS WEATHER UNDERGROUND API DOCS HERE: 
	// https://www.wunderground.com/weather/api/d/docs?MR=1
	return; 
}