import React, { Component } from 'react';
import { getConditions } from '../../api/weatherUnderground'

class Form extends Component {
  render() {
    return (
    	<div>Create a form here:</div>
    );
  }
}

export default Form;
